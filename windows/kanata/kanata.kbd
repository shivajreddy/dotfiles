(defsrc
  lctl lalt ralt
  caps
  h j k l ret
)

(deflayer default
  lalt lctl lalt
  @cap
  h j k l ret
)

(deflayer nav
  _    _    _
  _
  left down up right caps
)

(defalias
  cap (tap-hold 150 150 esc (layer-toggle nav))
)

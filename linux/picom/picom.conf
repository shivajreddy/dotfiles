# Force use of the OpenGL backend
backend = "glx";

# Blur settings
blur-method = "dual_kawase";
blur-strength = 5;  # Adjust blur intensity (1-10 range)

# Optional: Additional GLX optimizations
glx-no-stencil = true;
glx-copy-from-front = false;
